Subject: [PATCH] Material Design 3 popup, dialog and bottom sheet frames

Generated by buildscripts/tools/muse_patches.py regenerate.
Applied to the muse submodule working tree at configure time.
---
diff --git a/framework/uicomponents/qml/Muse/UiComponents/PopupPanel.qml b/framework/uicomponents/qml/Muse/UiComponents/PopupPanel.qml
index 59d948d..4e20abf 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/PopupPanel.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/PopupPanel.qml
@@ -38,12 +38,13 @@ Rectangle {
     signal opened()
     signal closed()
 
-    color: ui.theme.popupBackgroundColor
+    color: M3Roles.surfaceContainerHigh
     border.width: 1
-    border.color: ui.theme.strokeColor
+    border.color: M3Roles.outlineVariant
 
-    topLeftRadius: 20
-    topRightRadius: 20
+    //! NOTE Material Design 3 extra-large shape at the top of a bottom sheet
+    topLeftRadius: M3Roles.cornerExtraLarge
+    topRightRadius: M3Roles.cornerExtraLarge
 
     clip: true
 
@@ -135,9 +136,9 @@ Rectangle {
     Rectangle {
         anchors.fill: parent
 
-        color: Utils.colorWithAlpha(ui.theme.popupBackgroundColor, 0.75)
+        color: Utils.colorWithAlpha(M3Roles.surfaceContainerHigh, 0.75)
         border.width: 1
-        border.color: ui.theme.strokeColor
+        border.color: M3Roles.outlineVariant
 
         topLeftRadius: root.topLeftRadius
         topRightRadius: root.topRightRadius
diff --git a/framework/uicomponents/qml/Muse/UiComponents/StyledDialogView.qml b/framework/uicomponents/qml/Muse/UiComponents/StyledDialogView.qml
index ec2851f..4703b91 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/StyledDialogView.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/StyledDialogView.qml
@@ -41,6 +41,10 @@ DialogView {
 
     property bool closeOnEscape : true
 
+    //! NOTE Material Design 3 extra-large shape and level 3 elevation for dialogs
+    property real cornerRadius: M3Roles.cornerExtraLarge
+    property int elevationLevel: 3
+
     contentWidth: 240
     contentHeight: contentBody.childrenRect.height
 
@@ -94,16 +98,20 @@ DialogView {
         StyledRectangularShadow {
             anchors.fill: contentBackground
             visible: root.frameless
+
+            offset.y: root.elevationLevel
+            blur: root.elevationLevel * 4
+            color: Qt.rgba(M3Roles.shadow.r, M3Roles.shadow.g, M3Roles.shadow.b, 0.15 + root.elevationLevel * 0.03)
             radius: contentBackground.radius
         }
 
         Rectangle {
             id: contentBackground
             anchors.fill: parent
-            color: ui.theme.backgroundPrimaryColor
-            radius: root.frameless ? 4 : 0
+            color: M3Roles.surfaceContainerHigh
+            radius: root.frameless ? root.cornerRadius : 0
             border.width: root.frameless ? 1 : 0
-            border.color: ui.theme.strokeColor
+            border.color: M3Roles.outlineVariant
         }
 
         Item {
diff --git a/framework/uicomponents/qml/Muse/UiComponents/StyledPopupView.qml b/framework/uicomponents/qml/Muse/UiComponents/StyledPopupView.qml
index d0062e6..8f24027 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/StyledPopupView.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/StyledPopupView.qml
@@ -43,6 +43,14 @@ PopupView {
 
     property alias useDropShadow: content.useDropShadow
 
+    //! NOTE Material Design 3 medium shape for popup containers
+    property alias cornerRadius: content.cornerRadius
+    property alias elevationLevel: content.elevationLevel
+    property alias horizontalMargins: content.horizontalMargins
+    property alias verticalMargins: content.verticalMargins
+    property alias backgroundColor: content.backgroundColor
+    property alias borderColor: content.borderColor
+
     property bool isPlacementVertical:  (root.placementPolicies === PopupView.Default)
                                         || (root.placementPolicies & PopupView.PreferAbove)
                                         || (root.placementPolicies & PopupView.PreferBelow)
diff --git a/framework/uicomponents/qml/Muse/UiComponents/internal/PopupContent.qml b/framework/uicomponents/qml/Muse/UiComponents/internal/PopupContent.qml
index c4a4ab2..8956e9b 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/internal/PopupContent.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/internal/PopupContent.qml
@@ -40,6 +40,13 @@ FocusScope {
     property int padding: 0
     property int margins: 12
 
+    //! NOTE Allow asymmetric Material Design 3 padding, for example 8 by 4 for plain tooltips
+    property int horizontalMargins: root.margins
+    property int verticalMargins: root.margins
+
+    property color backgroundColor: M3Roles.surfaceContainerHigh
+    property color borderColor: M3Roles.outlineVariant
+
     property bool showArrow: false
     property int arrowX: 0
     property int arrowY: 0
@@ -49,6 +56,10 @@ FocusScope {
     property bool isOpened: false
     property bool useDropShadow: true
 
+    //! NOTE Material Design 3 shape and elevation of the popup container
+    property real cornerRadius: M3Roles.cornerMedium
+    property int elevationLevel: 3
+
     property bool animationEnabled: false
 
     property bool closeOnEscape: true
@@ -87,11 +98,11 @@ FocusScope {
         id: contentContainer
         x: root.padding
         y: root.padding
-        width: contentBody.width + root.margins * 2
-        height: contentBody.height + root.margins * 2
+        width: contentBody.width + root.horizontalMargins * 2
+        height: contentBody.height + root.verticalMargins * 2
 
-        implicitWidth: contentBody.implicitWidth + root.margins * 2
-        implicitHeight: contentBody.implicitHeight + root.margins * 2
+        implicitWidth: contentBody.implicitWidth + root.horizontalMargins * 2
+        implicitHeight: contentBody.implicitHeight + root.verticalMargins * 2
 
         scale: root.animationEnabled ? 0.7 : 1.0
         opacity: root.animationEnabled ? 0.5 : 1.0
@@ -101,7 +112,10 @@ FocusScope {
             anchors.fill: contentBackground
             visible: root.useDropShadow
 
-            blur: root.padding
+            //! NOTE Material Design 3 elevation levels 1 to 5
+            offset.y: root.elevationLevel
+            blur: Math.max(root.padding, root.elevationLevel * 3)
+            color: Qt.rgba(M3Roles.shadow.r, M3Roles.shadow.g, M3Roles.shadow.b, 0.15 + root.elevationLevel * 0.03)
             radius: contentBackground.radius
         }
 
@@ -109,13 +123,13 @@ FocusScope {
             id: contentBackground
             anchors.fill: parent
 
-            color: ui.theme.popupBackgroundColor
-            radius: 4
+            color: root.backgroundColor
+            radius: root.cornerRadius
 
             Item {
                 id: contentBody
-                x: root.margins
-                y: root.margins
+                x: root.horizontalMargins
+                y: root.verticalMargins
                 width: root.contentWidth
                 height: root.contentHeight
 
@@ -136,7 +150,7 @@ FocusScope {
             color: "transparent"
             radius: contentBackground.radius
             border.width: 1
-            border.color: ui.theme.strokeColor
+            border.color: root.borderColor
         }
 
         Canvas {
@@ -233,6 +247,11 @@ FocusScope {
     ]
 
     transitions: Transition {
-        NumberAnimation { properties: "scale, opacity"; easing.type: Easing.OutQuint; duration: root.animationEnabled ? 300 : 0 }
+        //! NOTE Material Design 3 standard-decelerate easing, disabled when reduced motion is requested
+        NumberAnimation {
+            properties: "scale, opacity"
+            easing.type: Easing.OutCubic
+            duration: root.animationEnabled ? M3Roles.duration(M3Roles.durationStandard) : 0
+        }
     }
 }
