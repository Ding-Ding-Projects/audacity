Subject: [PATCH] Material Design 3 buttons, selection controls, fields and tabs

Generated by buildscripts/tools/muse_patches.py regenerate.
Applied to the muse submodule working tree at configure time.
---
diff --git a/framework/uicomponents/qml/Muse/UiComponents/CheckBox.qml b/framework/uicomponents/qml/Muse/UiComponents/CheckBox.qml
index 41014c8..1e1ce4a 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/CheckBox.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/CheckBox.qml
@@ -77,21 +77,40 @@ FocusScope {
         Rectangle {
             id: box
 
-            height: 20
-            width: 20
+            //! NOTE Material Design 3 checkbox: 18 box, 2 corner radius, primary fill when selected
+            height: 18
+            width: 18
 
-            opacity: ui.theme.buttonOpacityNormal
+            opacity: 1.0
 
-            border.width: ui.theme.borderWidth
-            border.color: ui.theme.strokeColor
-            color: ui.theme.buttonColor
+            border.width: (root.checked || root.isIndeterminate) ? 0 : 2
+            border.color: M3Roles.onSurfaceVariant
+            color: (root.checked || root.isIndeterminate) ? M3Roles.primary : "transparent"
 
             radius: 2
 
             NavigationFocusBorder { navigationCtrl: navCtrl }
 
+            //! NOTE Material Design 3 state layer
+            Rectangle {
+                anchors.centerIn: parent
+                width: 40
+                height: 40
+                radius: 20
+
+                color: root.checked ? M3Roles.primary : M3Roles.onSurface
+                opacity: clickableArea.containsPress ? M3Roles.pressedOpacity
+                                                     : (clickableArea.containsMouse ? M3Roles.hoverOpacity
+                                                                                    : (navCtrl.active ? M3Roles.focusOpacity : 0.0))
+
+                Behavior on opacity {
+                    NumberAnimation { duration: M3Roles.duration(M3Roles.durationStandard); easing.type: Easing.OutCubic }
+                }
+            }
+
             StyledIconLabel {
                 anchors.fill: parent
+                color: M3Roles.onPrimary
                 iconCode: root.isIndeterminate ? IconCode.MINUS : IconCode.TICK_RIGHT_ANGLE
                 visible: root.checked || root.isIndeterminate
             }
@@ -129,25 +148,6 @@ FocusScope {
         }
     }
 
-    states: [
-        State {
-            name: "HOVERED"
-            when: clickableArea.containsMouse && !clickableArea.pressed
-
-            PropertyChanges {
-                target: box
-                opacity: ui.theme.buttonOpacityHover
-            }
-        },
-
-        State {
-            name: "PRESSED"
-            when: clickableArea.containsMouse && clickableArea.pressed
-
-            PropertyChanges {
-                target: box
-                opacity: ui.theme.buttonOpacityHit
-            }
-        }
-    ]
+    //! NOTE Material Design 3 draws the interaction feedback with a state layer,
+    //! so the box itself keeps a constant opacity.
 }
diff --git a/framework/uicomponents/qml/Muse/UiComponents/FlatButton.qml b/framework/uicomponents/qml/Muse/UiComponents/FlatButton.qml
index 8b0c932..9a90b49 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/FlatButton.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/FlatButton.qml
@@ -24,7 +24,7 @@ pragma ComponentBehavior: Bound
 import QtQuick
 import QtQuick.Layouts
 
-import Muse.Ui 
+import Muse.Ui
 import Muse.UiComponents
 
 /** APIDOC
@@ -70,22 +70,32 @@ FocusScope {
     property bool toolTipShowLocked: false
 
     property font iconFont: ui.theme.iconsFont
-    property color iconColor: ui.theme.fontPrimaryColor
+    property color iconColor: root.contentColor
     property font textFont: ui.theme.bodyFont
 
     property bool transparent: false
     property bool accentButton: false
 
+    //! NOTE Material Design 3 button variants:
+    //! - default: filled tonal button (secondary container)
+    //! - accentButton: filled button (primary)
+    //! - transparent: text button
+    property color contentColor: transparent ? M3Roles.primary
+                                             : accentButton ? M3Roles.onPrimary
+                                                            : M3Roles.onSecondaryContainer
+
     property color normalColor:
-        transparent ? "transparent" : accentButton ? accentColor : ui.theme.buttonColor
-    property color hoverHitColor: accentButton ? accentColor : ui.theme.buttonColor
-    property color accentColor: ui.theme.accentColor
+        transparent ? "transparent" : accentButton ? accentColor : M3Roles.secondaryContainer
+    property color hoverHitColor: root.contentColor
+    property color accentColor: M3Roles.primary
 
     property bool isNarrow: buttonType === FlatButton.Horizontal
     property real margins: isNarrow ? 12 : 16
     property real minWidth: isNarrow ? 24 : 132
 
-    property real backgroundRadius: 3
+    //! NOTE Material Design 3 uses a fully rounded shape and a 40 height for common buttons
+    property real buttonHeight: 40
+    property real backgroundRadius: root.buttonHeight / 2
 
     property bool drawFocusBorderInsideRect: false
 
@@ -140,7 +150,7 @@ FocusScope {
     objectName: root.text
 
     implicitWidth: contentLoader.itemImplicitWidth + 2 * margins
-    implicitHeight: Math.max(contentLoader.itemImplicitHeight, ui.theme.defaultButtonSize)
+    implicitHeight: Math.max(contentLoader.itemImplicitHeight, root.buttonHeight)
 
     opacity: root.enabled ? 1.0 : ui.theme.itemOpacityDisabled
 
@@ -180,40 +190,58 @@ FocusScope {
             id: background
 
             color: root.normalColor
-            opacity: ui.theme.buttonOpacityNormal
+            opacity: 1.0
 
             radius: root.backgroundRadius
-            border.width: ui.theme.borderWidth
-            border.color: ui.theme.strokeColor
+            border.width: root.transparent ? 0 : ui.theme.borderWidth
+            border.color: M3Roles.outlineVariant
 
             NavigationFocusBorder {
                 navigationCtrl: navCtrl
                 drawOutsideParent: !root.drawFocusBorderInsideRect
             }
 
-            states: [
-                State {
-                    name: "PRESSED"
-                    when: mouseArea.pressed
+            //! NOTE Material Design 3 state layer
+            Rectangle {
+                id: stateLayer
 
-                    PropertyChanges {
-                        target: background
-                        color: root.hoverHitColor
-                        opacity: ui.theme.buttonOpacityHit
-                    }
-                },
+                anchors.fill: parent
+                radius: background.radius
+
+                color: root.hoverHitColor
+                opacity: 0.0
+
+                states: [
+                    State {
+                        name: "PRESSED"
+                        when: mouseArea.pressed
+
+                        PropertyChanges { target: stateLayer; opacity: M3Roles.pressedOpacity }
+                    },
 
-                State {
-                    name: "HOVERED"
-                    when: mouseArea.containsMouse && !mouseArea.pressed
+                    State {
+                        name: "HOVERED"
+                        when: mouseArea.containsMouse && !mouseArea.pressed
 
-                    PropertyChanges {
-                        target: background
-                        color: root.hoverHitColor
-                        opacity: ui.theme.buttonOpacityHover
+                        PropertyChanges { target: stateLayer; opacity: M3Roles.hoverOpacity }
+                    },
+
+                    State {
+                        name: "FOCUSED"
+                        when: navCtrl.active
+
+                        PropertyChanges { target: stateLayer; opacity: M3Roles.focusOpacity }
+                    }
+                ]
+
+                transitions: Transition {
+                    NumberAnimation {
+                        property: "opacity"
+                        duration: M3Roles.duration(M3Roles.durationStandard)
+                        easing.type: Easing.OutCubic
                     }
                 }
-            ]
+            }
         }
     }
 
@@ -250,6 +278,7 @@ FocusScope {
                 Layout.alignment: Qt.AlignHCenter
                 text: root.text
                 font: root.textFont
+                color: root.contentColor
                 textFormat: root.textFormat
                 wrapMode: Text.Wrap
                 maximumLineCount: root.maximumLineCount
@@ -278,6 +307,7 @@ FocusScope {
                 Layout.alignment: Qt.AlignVCenter
                 text: root.text
                 font: root.textFont
+                color: root.contentColor
                 textFormat: root.textFormat
                 wrapMode: Text.Wrap
                 maximumLineCount: root.maximumLineCount
@@ -293,8 +323,8 @@ FocusScope {
 
             PropertyChanges {
                 target: root
-                implicitWidth: ui.theme.defaultButtonSize
-                implicitHeight: ui.theme.defaultButtonSize
+                implicitWidth: root.buttonHeight
+                implicitHeight: root.buttonHeight
             }
         },
 
@@ -306,7 +336,7 @@ FocusScope {
                 target: root
                 implicitWidth: Math.max(contentLoader.itemImplicitWidth + 2 * root.margins,
                                         root.minWidth)
-                implicitHeight: ui.theme.defaultButtonSize
+                implicitHeight: root.buttonHeight
             }
         },
 
@@ -316,7 +346,7 @@ FocusScope {
 
             PropertyChanges {
                 target: root
-                implicitHeight: ui.theme.defaultButtonSize
+                implicitHeight: root.buttonHeight
             }
         },
 
diff --git a/framework/uicomponents/qml/Muse/UiComponents/FlatToggleButton.qml b/framework/uicomponents/qml/Muse/UiComponents/FlatToggleButton.qml
index 043b0cd..c5ef3ea 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/FlatToggleButton.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/FlatToggleButton.qml
@@ -36,15 +36,20 @@ FocusScope {
     property string toolTipDescription: ""
     property string toolTipShortcut: ""
 
+    //! NOTE Material Design 3 icon toggle button
     property bool transparent: false
-    property color normalColor: transparent ? "transparent" : ui.theme.buttonColor
-    property color hoverHitColor: ui.theme.buttonColor
-    property color checkedColor: ui.theme.accentColor
+    property color normalColor: transparent ? "transparent" : M3Roles.secondaryContainer
+    property color hoverHitColor: root.contentColor
+    property color checkedColor: M3Roles.primary
+
+    readonly property color contentColor: root.checked ? M3Roles.onPrimary
+                                                       : (root.transparent ? M3Roles.onSurfaceVariant
+                                                                           : M3Roles.onSecondaryContainer)
 
     signal toggled()
 
-    implicitHeight: 30
-    implicitWidth: 30
+    implicitHeight: 40
+    implicitWidth: 40
 
     opacity: root.enabled ? 1.0 : ui.theme.itemOpacityDisabled
 
@@ -66,16 +71,32 @@ FocusScope {
         NavigationFocusBorder { navigationCtrl: navCtrl }
 
         color: root.checked ? root.checkedColor : root.normalColor
-        opacity: ui.theme.buttonOpacityNormal
+        opacity: 1.0
+
+        border.width: root.transparent ? 0 : ui.theme.borderWidth
+        border.color: M3Roles.outlineVariant
+        radius: width / 2
+
+        //! NOTE Material Design 3 state layer
+        Rectangle {
+            id: stateLayer
+
+            anchors.fill: parent
+            radius: backgroundRect.radius
 
-        border.width: ui.theme.borderWidth
-        border.color: ui.theme.strokeColor
-        radius: 2
+            color: root.hoverHitColor
+            opacity: 0.0
+
+            Behavior on opacity {
+                NumberAnimation { duration: M3Roles.duration(M3Roles.durationStandard); easing.type: Easing.OutCubic }
+            }
+        }
     }
 
     StyledIconLabel {
         id: buttonIcon
         anchors.fill: parent
+        color: root.contentColor
     }
 
     MouseArea {
@@ -113,9 +134,8 @@ FocusScope {
             when: mouseArea.containsMouse && !mouseArea.pressed
 
             PropertyChanges {
-                target: backgroundRect
-                color: root.checked ? root.checkedColor : root.hoverHitColor
-                opacity: ui.theme.buttonOpacityHover
+                target: stateLayer
+                opacity: M3Roles.hoverOpacity
             }
         },
 
@@ -124,9 +144,8 @@ FocusScope {
             when: mouseArea.pressed
 
             PropertyChanges {
-                target: backgroundRect
-                color: root.checked ? root.checkedColor : root.hoverHitColor
-                opacity: ui.theme.buttonOpacityHit
+                target: stateLayer
+                opacity: M3Roles.pressedOpacity
             }
         }
     ]
diff --git a/framework/uicomponents/qml/Muse/UiComponents/ProgressBar.qml b/framework/uicomponents/qml/Muse/UiComponents/ProgressBar.qml
index 64fc46e..d2af879 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/ProgressBar.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/ProgressBar.qml
@@ -78,13 +78,14 @@ FocusScope {
 
         anchors.fill: parent
 
-        color: ui.theme.backgroundSecondaryColor
+        //! NOTE Material Design 3 linear progress indicator
+        color: M3Roles.secondaryContainer
 
         NavigationFocusBorder { navigationCtrl: navCtrl }
 
-        radius: 3
-        border.color: ui.theme.strokeColor
-        border.width: Math.max(ui.theme.borderWidth, 1)
+        radius: height / 2
+        border.color: "transparent"
+        border.width: 0
 
         Item {
             anchors.fill: parent
@@ -104,7 +105,7 @@ FocusScope {
 
                 width: parent.width * Math.min(root.value / root.to, 1)
 
-                color: ui.theme.accentColor
+                color: M3Roles.primary
             }
         }
 
diff --git a/framework/uicomponents/qml/Muse/UiComponents/RoundedRadioButton.qml b/framework/uicomponents/qml/Muse/UiComponents/RoundedRadioButton.qml
index dac5664..5524c36 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/RoundedRadioButton.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/RoundedRadioButton.qml
@@ -111,13 +111,31 @@ RadioDelegate {
             height: width
             radius: 10
 
-            property real borderColorOpacity: ui.theme.buttonOpacityNormal
+            property real borderColorOpacity: 1.0
 
             NavigationFocusBorder { navigationCtrl: keynavCtrl }
 
-            color: ui.theme.textFieldColor
-            border.color: Utils.colorWithAlpha(ui.theme.fontPrimaryColor, borderColorOpacity)
-            border.width: 1
+            //! NOTE Material Design 3 radio button: 20 outline, 2 stroke, primary when selected
+            color: "transparent"
+            border.color: Utils.colorWithAlpha(root.checked ? M3Roles.primary : M3Roles.onSurfaceVariant, borderColorOpacity)
+            border.width: 2
+
+            //! NOTE Material Design 3 state layer
+            Rectangle {
+                anchors.centerIn: parent
+                width: 40
+                height: 40
+                radius: 20
+
+                color: root.checked ? M3Roles.primary : M3Roles.onSurface
+                opacity: root.pressed ? M3Roles.pressedOpacity
+                                      : (root.hovered ? M3Roles.hoverOpacity
+                                                      : (keynavCtrl.active ? M3Roles.focusOpacity : 0.0))
+
+                Behavior on opacity {
+                    NumberAnimation { duration: M3Roles.duration(M3Roles.durationStandard); easing.type: Easing.OutCubic }
+                }
+            }
         }
 
         Rectangle {
@@ -127,7 +145,7 @@ RadioDelegate {
             height: width
             radius: 5
 
-            color: ui.theme.accentColor
+            color: M3Roles.primary
             visible: root.checked
         }
     }
@@ -141,7 +159,7 @@ RadioDelegate {
 
             PropertyChanges {
                 target: backgroundRect
-                borderColorOpacity: ui.theme.buttonOpacityHit
+                borderColorOpacity: 1.0
             }
 
             PropertyChanges {
@@ -156,7 +174,7 @@ RadioDelegate {
 
             PropertyChanges {
                 target: backgroundRect
-                borderColorOpacity: ui.theme.buttonOpacityNormal
+                borderColorOpacity: 1.0
             }
 
             PropertyChanges {
@@ -171,7 +189,7 @@ RadioDelegate {
 
             PropertyChanges {
                 target: backgroundRect
-                borderColorOpacity: ui.theme.buttonOpacityHover
+                borderColorOpacity: 1.0
             }
         },
 
@@ -181,7 +199,7 @@ RadioDelegate {
 
             PropertyChanges {
                 target: backgroundRect
-                borderColorOpacity: ui.theme.buttonOpacityHover
+                borderColorOpacity: 1.0
             }
 
             PropertyChanges {
diff --git a/framework/uicomponents/qml/Muse/UiComponents/SearchField.qml b/framework/uicomponents/qml/Muse/UiComponents/SearchField.qml
index 5753ce6..5f40f41 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/SearchField.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/SearchField.qml
@@ -34,6 +34,10 @@ TextInputField {
 
     width: 220
 
+    //! NOTE Material Design 3 search field uses a fully rounded shape
+    backgroundRadius: root.height / 2
+    textSidePadding: 16
+
     hint: qsTrc("ui", "Search")
     hintIcon: IconCode.SEARCH
 
diff --git a/framework/uicomponents/qml/Muse/UiComponents/StyledBusyIndicator.qml b/framework/uicomponents/qml/Muse/UiComponents/StyledBusyIndicator.qml
index 5d85be3..8699b22 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/StyledBusyIndicator.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/StyledBusyIndicator.qml
@@ -22,12 +22,15 @@
 import QtQuick
 import QtQuick.Controls
 
+import Muse.Ui
+
 BusyIndicator {
     id: root
 
     padding: 0
 
-    property color backgroundColor: ui.theme.strokeColor
+    //! NOTE Material Design 3 circular progress indicator track and indicator
+    property color backgroundColor: M3Roles.secondaryContainer
 
     contentItem: Rectangle {
         id: backgroundCircle
@@ -47,7 +50,7 @@ BusyIndicator {
 
         RotationAnimator {
             target: foregroundCircle
-            running: root.visible && root.running
+            running: root.visible && root.running && !M3Roles.reducedMotion
             from: 0
             to: 360
             loops: Animation.Infinite
@@ -63,7 +66,7 @@ BusyIndicator {
                 context.arc(width / 2, height / 2,
                             width / 2 - backgroundCircle.border.width / 2,
                             Math.PI / -4, Math.PI / 4);
-                context.strokeStyle = ui.theme.accentColor;
+                context.strokeStyle = M3Roles.primary;
                 context.lineWidth = backgroundCircle.border.width;
                 context.stroke();
             }
diff --git a/framework/uicomponents/qml/Muse/UiComponents/StyledDropdown.qml b/framework/uicomponents/qml/Muse/UiComponents/StyledDropdown.qml
index 7cdc06b..10ec21b 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/StyledDropdown.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/StyledDropdown.qml
@@ -60,7 +60,8 @@ Item {
 
     signal activated(int index, var value)
 
-    height: 30
+    //! NOTE Material Design 3 outlined menu button metrics
+    height: 40
     width: 126
 
     function indexOfValue(value) {
@@ -146,13 +147,28 @@ Item {
         Rectangle {
             id: backgroundItem
             anchors.fill: parent
-            border.width: ui.theme.borderWidth
-            border.color: ui.theme.strokeColor
-            color: ui.theme.buttonColor
-            radius: 3
-            opacity: 0.7
+            border.width: 1
+            border.color: M3Roles.outline
+            color: "transparent"
+            radius: M3Roles.cornerExtraSmall
+            opacity: 1.0
 
             NavigationFocusBorder { navigationCtrl: navCtrl }
+
+            //! NOTE Material Design 3 state layer
+            Rectangle {
+                id: dropdownStateLayer
+
+                anchors.fill: parent
+                radius: backgroundItem.radius
+
+                color: mainItem.selected ? M3Roles.primary : M3Roles.onSurface
+                opacity: 0.0
+
+                Behavior on opacity {
+                    NumberAnimation { duration: M3Roles.duration(M3Roles.durationStandard); easing.type: Easing.OutCubic }
+                }
+            }
         }
 
         Loader {
@@ -262,9 +278,8 @@ Item {
                 when: mouseAreaItem.containsMouse && !mouseAreaItem.pressed
 
                 PropertyChanges {
-                    target: backgroundItem
-                    opacity: ui.theme.buttonOpacityHover
-                    color: mainItem.hoveredColor
+                    target: dropdownStateLayer
+                    opacity: M3Roles.hoverOpacity
                 }
             },
 
@@ -273,8 +288,8 @@ Item {
                 when: mouseAreaItem.pressed
 
                 PropertyChanges {
-                    target: backgroundItem
-                    opacity: ui.theme.buttonOpacityHit
+                    target: dropdownStateLayer
+                    opacity: M3Roles.pressedOpacity
                 }
             },
 
@@ -283,9 +298,8 @@ Item {
                 when: mainItem.selected
 
                 PropertyChanges {
-                    target: backgroundItem
-                    opacity: ui.theme.accentOpacityHit
-                    color: ui.theme.accentColor
+                    target: dropdownStateLayer
+                    opacity: M3Roles.pressedOpacity
                 }
             }
         ]
diff --git a/framework/uicomponents/qml/Muse/UiComponents/StyledSlider.qml b/framework/uicomponents/qml/Muse/UiComponents/StyledSlider.qml
index 86c5b1d..62df964 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/StyledSlider.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/StyledSlider.qml
@@ -35,6 +35,7 @@ Slider {
     implicitWidth: vertical ? prv.handleSize : prv.defaultLength
     implicitHeight: vertical ? prv.defaultLength : prv.handleSize
 
+
     hoverEnabled: root.enabled
     wheelEnabled: true
 
@@ -43,9 +44,11 @@ Slider {
     QtObject {
         id: prv
 
+        //! NOTE Material Design 3 slider: 4 track, fully rounded, 20 by 4 handle
         readonly property int lineSize: 4
-        readonly property int radius: 4
-        readonly property int handleSize: 14
+        readonly property int radius: 2
+        readonly property int handleSize: 20
+        readonly property int handleWidth: 4
         readonly property int defaultLength: 220
     }
 
@@ -97,7 +100,7 @@ Slider {
             visible: root.fillBackground
 
             opacity: 1
-            color: ui.theme.accentColor
+            color: M3Roles.primary
             radius: prv.radius
         }
 
@@ -111,8 +114,8 @@ Slider {
 
             x: filledBackground.width
 
-            opacity: 0.5
-            color: ui.theme.fontPrimaryColor
+            opacity: 1
+            color: M3Roles.secondaryContainer
             radius: prv.radius
         }
     }
@@ -123,43 +126,29 @@ Slider {
         x: root.leftPadding + root.visualPosition * (root.availableWidth - width)
         y: root.topPadding + root.availableHeight / 2 - height / 2
 
-        width: prv.handleSize
-        height: width
-        radius: width / 2
+        width: root.vertical ? prv.handleSize : prv.handleWidth
+        height: root.vertical ? prv.handleWidth : prv.handleSize
+        radius: Math.min(width, height) / 2
 
-        color: ui.theme.textFieldColor
+        color: M3Roles.primary
 
+        //! NOTE Material Design 3 state layer around the handle
         Rectangle {
-            id: handleBorder
-
-            anchors.fill: parent
-
-            radius: width / 2
-            opacity: 0.7
-            color: "transparent"
-            border.width: 1
-            border.color: ui.theme.fontPrimaryColor
-
-            states: [
-                State {
-                    name: "HOVERED"
-                    when: root.hovered && !root.pressed
-
-                    PropertyChanges {
-                        target: handleBorder
-                        opacity: 0.5
-                    }
-                },
-                State {
-                    name: "PRESSED"
-                    when: root.pressed
-
-                    PropertyChanges {
-                        target: handleBorder
-                        opacity: 1
-                    }
-                }
-            ]
+            id: handleStateLayer
+
+            anchors.centerIn: parent
+            width: 40
+            height: 40
+            radius: 20
+
+            color: M3Roles.primary
+            opacity: root.pressed ? M3Roles.pressedOpacity
+                                  : (root.hovered ? M3Roles.hoverOpacity
+                                                  : (navCtrl.active ? M3Roles.focusOpacity : 0.0))
+
+            Behavior on opacity {
+                NumberAnimation { duration: M3Roles.duration(M3Roles.durationStandard); easing.type: Easing.OutCubic }
+            }
         }
     }
 
diff --git a/framework/uicomponents/qml/Muse/UiComponents/StyledTabBar.qml b/framework/uicomponents/qml/Muse/UiComponents/StyledTabBar.qml
index fa08aaa..d5c7237 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/StyledTabBar.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/StyledTabBar.qml
@@ -28,10 +28,20 @@ import Muse.UiComponents
 TabBar {
     id: root
 
-    spacing: 36
+    //! NOTE Material Design 3 secondary tabs
+    spacing: 0
 
-    background: Item {
-        implicitHeight: 32
+    background: Rectangle {
+        implicitHeight: 48
+        color: M3Roles.surface
+
+        Rectangle {
+            anchors.left: parent.left
+            anchors.right: parent.right
+            anchors.bottom: parent.bottom
+            height: 1
+            color: M3Roles.outlineVariant
+        }
     }
 
     function focusOnTab(index) {
diff --git a/framework/uicomponents/qml/Muse/UiComponents/StyledTabButton.qml b/framework/uicomponents/qml/Muse/UiComponents/StyledTabButton.qml
index 1d9ace1..133e3c4 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/StyledTabButton.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/StyledTabButton.qml
@@ -47,8 +47,9 @@ TabButton {
            ? (tabBar.width - (tabBar.count - 1) * tabBar.spacing) / tabBar.count
            : implicitWidth
 
-    leftPadding: 0
-    rightPadding: 0
+    //! NOTE Material Design 3 secondary tab metrics
+    leftPadding: 16
+    rightPadding: 16
 
     font: isCurrent ? ui.theme.largeBodyBoldFont : ui.theme.largeBodyFont
 
@@ -85,15 +86,30 @@ TabButton {
         id: textLabel
         text: root.text
         font: root.font
-        opacity: 0.75
+        color: root.isCurrent ? M3Roles.onSurface : M3Roles.onSurfaceVariant
+        opacity: 1.0
         displayTruncatedTextOnHover: true
     }
 
     background: Item {
-        implicitHeight: 32
+        implicitHeight: 48
 
         NavigationFocusBorder { navigationCtrl: navCtrl }
 
+        //! NOTE Material Design 3 state layer
+        Rectangle {
+            anchors.fill: parent
+
+            color: M3Roles.onSurface
+            opacity: root.pressed ? M3Roles.pressedOpacity
+                                  : (root.hovered ? M3Roles.hoverOpacity
+                                                  : (navCtrl.active ? M3Roles.focusOpacity : 0.0))
+
+            Behavior on opacity {
+                NumberAnimation { duration: M3Roles.duration(M3Roles.durationStandard); easing.type: Easing.OutCubic }
+            }
+        }
+
         Rectangle {
             id: selectedUnderline
 
@@ -101,32 +117,14 @@ TabButton {
             anchors.right: parent.right
             anchors.bottom: parent.bottom
 
+            //! NOTE Material Design 3 secondary tab indicator
             height: 2
 
-            visible: isCurrent
-            color: ui.theme.accentColor
+            visible: root.isCurrent
+            color: M3Roles.primary
         }
     }
 
-    states: [
-        State {
-            name: "HOVERED"
-            when: root.hovered && !root.isCurrent
-
-            PropertyChanges {
-                target: contentItem
-                opacity: 1
-            }
-        },
-
-        State {
-            name: "SELECTED"
-            when: root.isCurrent
-
-            PropertyChanges {
-                target: textLabel
-                opacity: 1
-            }
-        }
-    ]
+    //! NOTE Material Design 3 draws hover and selection feedback with the
+    //! state layer and the indicator, so no extra opacity states are needed.
 }
diff --git a/framework/uicomponents/qml/Muse/UiComponents/TextInputField.qml b/framework/uicomponents/qml/Muse/UiComponents/TextInputField.qml
index ff76055..3ba8d39 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/TextInputField.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/TextInputField.qml
@@ -47,6 +47,8 @@ FocusScope {
     property alias readOnly: valueInput.readOnly
 
     property real textSidePadding: 12
+    //! NOTE Material Design 3 shape of the field container
+    property real backgroundRadius: M3Roles.cornerExtraSmall
     property real accessoriesPadding: 4
 
     readonly property alias inputField: valueInput
@@ -95,7 +97,8 @@ FocusScope {
         }
     }
 
-    implicitHeight: 30
+    //! NOTE Material Design 3 outlined text field
+    implicitHeight: 40
     implicitWidth: parent.width
 
     opacity: root.enabled ? 1.0 : ui.theme.itemOpacityDisabled
@@ -135,10 +138,10 @@ FocusScope {
 
         NavigationFocusBorder { navigationCtrl: navCtrl }
 
-        color: ui.theme.textFieldColor
-        border.color: ui.theme.strokeColor
-        border.width: Math.max(ui.theme.borderWidth, 1)
-        radius: 3
+        color: "transparent"
+        border.color: M3Roles.outline
+        border.width: 1
+        radius: root.backgroundRadius
     }
 
     RowLayout {
@@ -318,13 +321,13 @@ FocusScope {
         State {
             name: "HOVERED"
             when: root.containsMouse && !valueInput.activeFocus
-            PropertyChanges { target: background; border.color: Utils.colorWithAlpha(ui.theme.accentColor, 0.6) }
+            PropertyChanges { target: background; border.color: M3Roles.onSurface }
         },
 
         State {
             name: "FOCUSED"
             when: valueInput.activeFocus
-            PropertyChanges { target: background; border.color: ui.theme.accentColor }
+            PropertyChanges { target: background; border.color: M3Roles.primary; border.width: 2 }
         }
     ]
 
