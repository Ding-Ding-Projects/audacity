Subject: [PATCH] Let a button box hold a host application button component

Generated by buildscripts/tools/muse_patches.py regenerate.
Applied to the muse submodule working tree at configure time.
---
diff --git a/framework/uicomponents/qml/Muse/UiComponents/ButtonBox.qml b/framework/uicomponents/qml/Muse/UiComponents/ButtonBox.qml
index 4f15f98..22e4ad9 100644
--- a/framework/uicomponents/qml/Muse/UiComponents/ButtonBox.qml
+++ b/framework/uicomponents/qml/Muse/UiComponents/ButtonBox.qml
@@ -27,6 +27,13 @@ import QtQuick.Layouts
 import Muse.Ui
 import Muse.UiComponents
 
+/*
+ * NOTE A button box reads buttonId, buttonRole, isLeftSide, accentButton and
+ * navigation from its children through untyped property access rather than
+ * through a FlatButton cast, so that a host application can put its own button
+ * component in a button box and keep the platform button order, the accept and
+ * reject defaults and the navigation panel.
+ */
 Container {
     id: root
 
@@ -71,7 +78,7 @@ Container {
 
     function accentButton() {
         for (var i = 0; i < root.count; i++) {
-            var btn = root.itemAt(i) as FlatButton
+            var btn = root.itemAt(i)
             if (btn.accentButton) {
                 return btn
             }
@@ -82,6 +89,7 @@ Container {
 
     function addButton(text, buttonId, buttonRole, isAccent, isLeftSide) {
         const button = Qt.createQmlObject("import Muse.UiComponents; FlatButton {}", root) as FlatButton
+        //! NOTE See the note below about untyped button access
         button.text = text
         button.accentButton = isAccent
 
@@ -100,7 +108,7 @@ Container {
 
     function restoreAccessibility() {
         for (var i = 0; i < root.count; i++) {
-            var btn = root.itemAt(i) as FlatButton
+            var btn = root.itemAt(i)
             if (!Boolean(btn.navigation)) {
                 continue
             }
@@ -160,7 +168,7 @@ Container {
                     continue
                 }
 
-                var btn = root.itemAt(i) as FlatButton
+                var btn = root.itemAt(i)
                 if (buttonInfo.button !== btn) {
                     root.moveItem(buttonInfo.index, i)
                 }
@@ -192,7 +200,7 @@ Container {
 
         function buttonInfo(buttonType) {
             for (var i = 0; i < root.count; i++) {
-                var btn = root.itemAt(i) as FlatButton
+                var btn = root.itemAt(i)
                 if (!Boolean(btn)) {
                     continue
                 }
@@ -236,7 +244,7 @@ Container {
             root.addItem(separator)
 
             for (var i = root.count - 2; i >= 0; i--) {
-                var button = root.itemAt(i) as FlatButton
+                var button = root.itemAt(i)
                 if (button.buttonId === lastLeftSideButtonType) {
                     break;
                 }
