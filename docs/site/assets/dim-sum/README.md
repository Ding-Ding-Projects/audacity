# Bundled dim-sum illustration

`hk-dish-0001-classic-har-gow.png` is the existing Classic Har Gow (蝦餃)
catalog illustration. Its original catalog identifies it as AI-generated.
It is not a camera-origin photograph and must not be described as one.

The project owner explicitly selected reuse of this existing generated catalog
image with accurate origin disclosure. No image generation or network download
occurs during release publication. The file was copied unchanged from the
existing catalog archive and verified against its recorded SHA-256.

The adjacent index records its exact size, decoded dimensions, origin, and digest.
Release notes and any visible use must retain the generated-origin disclosure.
